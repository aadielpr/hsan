# Hsan

Game site where kids play and learn.

## Target devices

- Browser, either on phone, tablet, or pc

## Run locally

```bash
bun install
bun dev
```

Then open the printed URL. Use `--host` if you want to test from a phone or tablet on the same network:

```bash
bun dev --host
```

## Build

```bash
bun build
```
